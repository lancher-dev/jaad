---
/**
 * @deprecated Define a local Astro layout for pages outside the documentation.
 * This compatibility layout will be removed in JAAD 0.8.
 */
import Page from "./Page.astro";

interface Props {
  title?: string;
  description?: string;
  bare?: boolean;
}
---

<Page {...Astro.props}>
  <slot />
</Page>
